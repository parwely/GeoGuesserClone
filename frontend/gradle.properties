# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx6144m -XX:+UseG1GC -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Enable Gradle configuration cache
org.gradle.configuration-cache=true

# Enable Gradle build cache
org.gradle.caching=true

# JVM args for better performance - Optimiert für moderne Multi-Core CPUs
org.gradle.jvmargs=-Xmx6144m -XX:+UseG1GC -XX:MaxMetaspaceSize=1024m -XX:+UseStringDeduplication

# Enable incremental compilation
kotlin.incremental=true
kotlin.incremental.useClasspathSnapshot=true

# R8 Full Mode - Aktiviert aggressive Code-Optimierung
android.enableR8.fullMode=true

# Non-transitive R classes - Reduziert R.class Größe
android.nonTransitiveRClass=true

# Disable unnecessary features for faster builds
android.enableJetifier=false

# Build performance optimizations
org.gradle.daemon=true
org.gradle.configureondemand=true
org.gradle.workers.max=6

# File system watching für bessere inkrementelle Builds
org.gradle.vfs.watch=true

# Memory optimization
android.experimental.enableSourceSetPathsMap=true

# Kotlin compilation optimization
kotlin.build.report.output=file

# Network optimizations
systemProp.org.gradle.internal.http.connectionTimeout=120000
systemProp.org.gradle.internal.http.socketTimeout=120000

# Compose compiler optimizations - Entfernte veraltete Optionen
android.experimental.enableArtProfiles=true
